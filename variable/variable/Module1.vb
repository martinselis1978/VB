Module Module1

    Dim num As Integer = 12 'declarando una variable por fuera se puede operar dentro de cualquier metodo
    Sub Main()

        num = 22 'aca el ejemplo explicado arriba

        'CaseInsensitiveComparer distingue entre mayusculas y minusculas

        Dim numero As Integer = 12 'ingreso de variables modificables

        Dim letra As Char = "a" 'ingreso de variables modificables tipo caracter entre ""

        Dim letras As String = "a" 'ingreso de variables modificables tipo string entre ""

        Const PULGADA As Double = 2.54 'ingreso de variables constantes (const)

        Console.ReadKey()
    End Sub

End Module
